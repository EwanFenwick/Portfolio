namespace Portfolio.EventBusSystem {
    public enum PauseEventType {
        None,
        Unpause,
        Dialogue,
    }

    public enum PlayerStatePhase {
        Entered,
        Exited,
    }
}